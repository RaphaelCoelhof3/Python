número = int(input("Entre com um número inteiro:"))
if (número % 2 == 0): #um número é par quando o resto da divisão por 2 é semelhante a 0.
    print("par")
else: #Caso contrário, se o reto da divisão por 2 não for igual a 0 significa que ele é um número impar.
    print("ímpar")
